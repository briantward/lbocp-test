package main

import "fmt"
import "net/http"
import "log"

func main() {
	// TODO: get HTTP Server pods available in namespace
	// curl route endpoint with cookies set, in loop
	// capture results, confirm cookie being used or not
}
